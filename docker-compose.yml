version: '3'

services:

  app:
    build: ./infra/php
    volumes:
      - ./src:/data

  web:
    image: nginx:1.22
    ports:
      - 9999:80
    volumes:
      - ./src:/data
      - ./infra/nginx/default.conf:/etc/nginx/conf.d/default.conf
    working_dir: /data
